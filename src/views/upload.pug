extends index

block subcontent
    <!-- FILE INPUT FORM -->  
    div.form  
      form(action='convert' method='post' enctype="multipart/form-data")
        div.file
          label(for='imgpicker' )
          | Choose your photo
          input#imgpicker(type="file" name= "img" accept="image/png, image/jpeg" capture="environment")
        div.button
          input#file_submit(type="submit" value="Let's do this")